# 更新日志

本项目遵循语义化版本；每条记录写「改了什么 + 为什么」，不写流水账。

## 1.0.0 — 首个可用版本

### 新增

- **九阶段流水线**：取数 → 清洗 → 体检 → 指标 → 诊断 → 图表 → 调仓 → 实验 → 成稿，
  每阶段独立可测、产物落盘（`adops/pipeline.py`）。
- **两种输入布局自动识别**：曝光级日志与聚合报表走同一套口径，出口同构
  （`adops/collect/schema.py`、`adops/clean/normalize.py`）。
- **列名别名映射**：覆盖巨量/腾讯/手工表等常见列名（含中文），未识别列如实记入审计
  而不参与计算。
- **数据质量体检**：12 项检查（完整率 / 重复率 / 一致性 / 消耗异常 / 时间覆盖 / 归因
  覆盖率），输出 `ok / warn / fail` 与阈值依据。
- **13 条确定性诊断规则**（`adops/diagnose/rules.py`）：全部带样本量门槛，比率类问题
  额外做显著性检验，时段类问题做 Bonferroni 多重比较校正。
- **统计层零依赖实现**（`adops/experiment/tests_stats.py`）：两比例 z 检验、Wilson
  区间、样本量/MDE/功效互算、O'Brien-Fleming 序贯边界，只用标准库，公式写在代码里。
- **预算再分配模拟**（`adops/metrics/allocation.py`）：确定性算法，输出「先试哪个方向」，
  并把一阶近似假设随结果一起返回。
- **AI 叙述层 + 数字回指守卫**（`adops/ai/`）：结论由规则产出，AI 只负责表达；模型
  输出里每个数字都必须能在证据表中按容差匹配，否则整段作废、回落到确定性模板。
  零密钥可跑，报告照常产出。
- **报告与复核 SQL**：报告含证据/动作/影响面三段式发现清单、口径附录与数据边界声明；
  附 6 段与结论口径完全一致的 SQL，便于业务方在数仓里自行复核。
- **离线夹具（真实公开数据）**：`examples/sample_data/` 两份夹具来自 Criteo Attribution
  Modeling for Bidding Dataset 的 16,468,027 次真实曝光，由 `scripts/build_fixture.py`
  可复现生成；许可与数据边界见该目录 README。
- **测试**：191 项离线测试，覆盖纯函数边界、口径纪律、规则触发与沉默、统计已知值、
  AI 守卫、报告结构与流水线确定性；CI 在 Python 3.9 / 3.11 / 3.12 上跑通，并额外断言
  「结论中无不可回指数字」与「报告保留数据边界声明」。

### 被真实数据逼出来的两处修正（值得单独记）

- **消耗异常检测从「按明细行」改为「按日汇总」**：单次曝光的价格由竞价瞬时决定，
  天然离散长尾，逐条做 IQR 检测在真实数据上误报 7.09%，会把正常数据判成异常。
- **去重必须包含 `user_id`**：早期版本把 `user_id` 排除在去重键之外，同一时刻两条
  不同用户的曝光被误判为重复，产生 2.76% 的假重复率，整份数据被判成「不可用」。
